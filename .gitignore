# Claude Code internal instructions (not for public repo)
CLAUDE.md

# docs/ is tracked (screenshots, write-ups)

# Python virtual environments
.venv-convert/
.venv/
venv/
__pycache__/
*.pyc
*.pyo

# Model files (large binaries, push separately)
models/

# Benchmark results: commit curated top-level CSVs, ignore local pull artifacts
# (adb pull nests data under results/raw/results/ — that copy stays ignored)
results/raw/*
!results/raw/*.csv
results/graphs/

# ImageNet dataset (large, distribute separately)
data/

# Android build outputs
app/build/
*.apk
*.aab

# Android Studio / IntelliJ
.idea/
*.iml
local.properties

# Gradle
.gradle/
build/

# macOS
.DS_Store

# Windows
Thumbs.db
