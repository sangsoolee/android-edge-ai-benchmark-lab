android.useAndroidX=true
android.suppressUnsupportedCompileSdk=35
# Note: do NOT set org.gradle.java.home here — it's machine-specific and breaks CI
# and other machines. Gradle uses JAVA_HOME / the toolchain instead.
org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=512m
